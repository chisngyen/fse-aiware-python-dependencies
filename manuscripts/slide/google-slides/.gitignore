# Rendered previews / crops (regenerate via: node render-slide.js all)
slide_*.png
before_*.png
*_before.png
_*.png
_render.html
_render_*.html

# Unused intermediate paste images (not referenced by the deck)
uploads/pasted-*.png

# Scratch backups & parallel-agent work copies
*.backup*.dc.html
work_*.dc.html
chk_*.png

# Heavy / stale exports (the .pptx is regenerated by Claude-design, not from this source)
export/
uploads/*.pdf
.thumbnail
