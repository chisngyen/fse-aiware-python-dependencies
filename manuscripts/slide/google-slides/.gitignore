# Render scratch only. Preview PNGs (slide_*.png / before_*.png) are kept
# VISIBLE in the folder for review (untracked; regenerate via: node render-slide.js all)
_cube*.png
_render.html
_render_*.html

# Unused intermediate paste images (not referenced by the deck)
uploads/pasted-*.png

# Scratch backups & parallel-agent work copies
*.backup*.dc.html
work_*.dc.html
chk_*.png

# Heavy / stale exports (the .pptx is regenerated by Claude-design, not from this source)
export/
uploads/*.pdf
.thumbnail

# Embedded video (heavy; keep local)
uploads/*.mp4
